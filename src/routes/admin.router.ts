// import { Router } from 'express';
// import { authMiddleware } from '../middleware/auth.middleware.js';
// import { requireAdmin } from '../middleware/role.middleware.js';
// import { approveSellerRequest, rejectSellerRequest } from '../controllers/seller.controller.js';

// export const adminRouter = Router();

// adminRouter.post('/seller-requests/:userId/approve', authMiddleware, requireAdmin(), approveSellerRequest);
// adminRouter.post('/seller-requests/:userId/reject', authMiddleware, requireAdmin(), rejectSellerRequest);
